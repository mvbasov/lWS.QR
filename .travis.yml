language: android
android:
  components:
    - tools
    - platform-tools
    # The BuildTools version used by your project
    - build-tools-28.0.2
    # The SDK version used to compile your project
    - android-26
  script:
    - ./gradlew wrapper --gradle-version=4.6 build connectedCheck
